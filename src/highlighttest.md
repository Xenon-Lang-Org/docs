

```shell
if else salut ça va while for
#woaw 
nothing
```

```mermaid
graph TD;
    A-->B;
    A-->C;
    B-->D;
    C-->D;
```

```myLanguage
// hello i'm comment
if (x == 42) {
    console.log("Hello World!");
}
```

```rust
fn is_prime(nb: u32) -> bool
{
    let mut i: u32 = 2;

    if (nb == 0 || nb == 1) {
        return false;
    }
    while (i < nb) {
        if (nb % i == 0) {
            return false;
        }
        i += 1;
    }
    return true;
}
```